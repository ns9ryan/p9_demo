// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"oa.98ent.com/p9/node-dispatch/rpc/ent/dispatchtask"
)

// 调度任务表
type DispatchTask struct {
	config `json:"-"`
	// ID of the ent.
	// 主键ID
	ID int64 `json:"id,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 更新时间
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 调度中心生成的全局唯一任务编号
	TaskNo string `json:"task_no,omitempty"`
	// 调用方生成的请求编号
	RequestNo string `json:"request_no,omitempty"`
	// 任务目标服务
	Target string `json:"target,omitempty"`
	// 任务类型
	TaskType string `json:"task_type,omitempty"`
	// 任务参数
	Params json.RawMessage `json:"params,omitempty"`
	// 任务状态: 1待执行, 2执行中, 3成功, 4失败
	Status int64 `json:"status,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DispatchTaskQuery when eager-loading is set.
	Edges        DispatchTaskEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DispatchTaskEdges holds the relations/edges for other nodes in the graph.
type DispatchTaskEdges struct {
	// Runs holds the value of the runs edge.
	Runs []*DispatchTaskRun `json:"runs,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RunsOrErr returns the Runs value or an error if the edge
// was not loaded in eager-loading.
func (e DispatchTaskEdges) RunsOrErr() ([]*DispatchTaskRun, error) {
	if e.loadedTypes[0] {
		return e.Runs, nil
	}
	return nil, &NotLoadedError{edge: "runs"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DispatchTask) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case dispatchtask.FieldParams:
			values[i] = new([]byte)
		case dispatchtask.FieldID, dispatchtask.FieldStatus:
			values[i] = new(sql.NullInt64)
		case dispatchtask.FieldTaskNo, dispatchtask.FieldRequestNo, dispatchtask.FieldTarget, dispatchtask.FieldTaskType:
			values[i] = new(sql.NullString)
		case dispatchtask.FieldCreatedAt, dispatchtask.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DispatchTask fields.
func (_m *DispatchTask) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dispatchtask.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case dispatchtask.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case dispatchtask.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case dispatchtask.FieldTaskNo:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field task_no", values[i])
			} else if value.Valid {
				_m.TaskNo = value.String
			}
		case dispatchtask.FieldRequestNo:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field request_no", values[i])
			} else if value.Valid {
				_m.RequestNo = value.String
			}
		case dispatchtask.FieldTarget:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field target", values[i])
			} else if value.Valid {
				_m.Target = value.String
			}
		case dispatchtask.FieldTaskType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field task_type", values[i])
			} else if value.Valid {
				_m.TaskType = value.String
			}
		case dispatchtask.FieldParams:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field params", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Params); err != nil {
					return fmt.Errorf("unmarshal field params: %w", err)
				}
			}
		case dispatchtask.FieldStatus:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.Int64
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DispatchTask.
// This includes values selected through modifiers, order, etc.
func (_m *DispatchTask) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRuns queries the "runs" edge of the DispatchTask entity.
func (_m *DispatchTask) QueryRuns() *DispatchTaskRunQuery {
	return NewDispatchTaskClient(_m.config).QueryRuns(_m)
}

// Update returns a builder for updating this DispatchTask.
// Note that you need to call DispatchTask.Unwrap() before calling this method if this DispatchTask
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DispatchTask) Update() *DispatchTaskUpdateOne {
	return NewDispatchTaskClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DispatchTask entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DispatchTask) Unwrap() *DispatchTask {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DispatchTask is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DispatchTask) String() string {
	var builder strings.Builder
	builder.WriteString("DispatchTask(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("task_no=")
	builder.WriteString(_m.TaskNo)
	builder.WriteString(", ")
	builder.WriteString("request_no=")
	builder.WriteString(_m.RequestNo)
	builder.WriteString(", ")
	builder.WriteString("target=")
	builder.WriteString(_m.Target)
	builder.WriteString(", ")
	builder.WriteString("task_type=")
	builder.WriteString(_m.TaskType)
	builder.WriteString(", ")
	builder.WriteString("params=")
	builder.WriteString(fmt.Sprintf("%v", _m.Params))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteByte(')')
	return builder.String()
}

// DispatchTasks is a parsable slice of DispatchTask.
type DispatchTasks []*DispatchTask
